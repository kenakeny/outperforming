# data ingestion + eda + modeling notebooks. versions match the tested env (python 3.10).
pandas>=2.0
numpy>=1.26,<2.0
yfinance>=0.2.60
financedatabase>=2.0    # the etf/fund universe + categories
PyYAML>=6.0
requests>=2.32
pyarrow>=15.0
matplotlib>=3.8
seaborn>=0.13
scikit-learn>=1.7
xgboost>=3.2
catboost>=1.2
jupyter_client>=8.6     # notebook execution (run_notebooks.py)
nbconvert>=7.17
nbformat>=5.10
torch>=2.5              # the LSTM in train_models.py (cu121 build for GPU)
joblib>=1.3             # persists the logistic-regression baseline pipeline

# serving (serve/)
fastapi>=0.115
uvicorn>=0.30
streamlit>=1.38
python-multipart>=0.0.9  # UploadFile support for /predict/csv

# tests
pytest>=8.0
httpx>=0.27             # fastapi.testclient


[pytest]
testpaths = tests
addopts = -q --strict-markers -m "not slow"
markers =
    slow: rebuilds the full feature panel (minutes). Run with: pytest -m slow
filterwarnings =
    ignore::DeprecationWarning
    ignore::FutureWarning
